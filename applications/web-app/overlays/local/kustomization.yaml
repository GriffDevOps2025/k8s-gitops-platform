apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../base

commonLabels:
  environment: local

patchesStrategicMerge:
  - service-loadbalancer.yaml

images:
  - name: nginx
    newTag: "1.25-alpine"