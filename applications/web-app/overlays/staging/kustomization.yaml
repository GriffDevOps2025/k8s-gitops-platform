apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../base

commonLabels:
  environment: staging

patchesStrategicMerge:
  - replica-patch.yaml
  - resource-patch.yaml

images:
  - name: nginx
    newTag: "1.25-alpine"